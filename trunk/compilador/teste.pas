program numerosPerfeitos;
 
var ate, x, soma, i: integer;
 
begin
	x:=0;
	x:=x+1;
	soma:=0;
	for i:= 1 to x-1 do
	  begin
	  if x mod i = 0 then soma:=soma + i;
	  end
	if soma = x then
	 begin
	 end
end ERRO
